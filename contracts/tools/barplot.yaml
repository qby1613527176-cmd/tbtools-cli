name: barplot
kind: bridge
mode: bridge
class: BarplotCli
xmx: 3g
runner: plot
group: expr
help: 'barplot: barplot <enrichment.tsv> <out> <termCol> <pvalCol> [classCol'
capabilities:
- expression
capabilities_ontology:
- expression
relations:
  accepts:
  - EXPRESSION_TABLE
  produces:
  - PLOT
  related_to:
  - volcano
  - heatmap
inputs:
- name: enrichment
  role: file
  format: tsv
  required: true
  note: 富集表
  columns:
  - Term
  - Pvalue
outputs:
- svg
