name: sranum2info
kind: direct
mode: direct
class: biocjava.bioWeb.EntrezUtils.BatchGetSRARecordInfo
xmx: 2g
runner: java
group: table
help: 'sranum2info --sraIdList <srrList.txt> --outTabInfo <out.xls>   # SRR 批量信息表（GUI
  逆向 #41 BatchGetSRARecordInfo；⚠️ 联网 NCBI Entrez+限速；自带 ArgsParser；SRA 组 3/3 全清）'
capabilities:
- table_operations
capabilities_ontology:
- table
- table_operations
relations:
  accepts:
  - TABLE
  produces:
  - TABLE_OUT
