name: peakdist
kind: bridge
mode: bridge
class: PeakDistCli
xmx: 3g
runner: plot
group: chipseq
help: 'peakdist: peakdist <chrLen.tsv> <macs2_peak.xls> <out> [--chrHeight H]'
capabilities:
- chip_seq
capabilities_ontology:
- chip-seq
- chip_seq
relations:
  accepts:
  - ALIGNMENT
  - PEAKS
  produces:
  - PEAK_ANNOTATION
