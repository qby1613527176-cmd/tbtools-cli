name: muscle
kind: manual
mode: manual
class: ''
xmx: 2g
runner: plot
group: seq
help: 'muscle: muscle <in.fa> <out.aln> [--super5] [--threads N]   # MUSCLE 多序列比对（GUI
  逆向 #27 MuscleGUIPanel→QuickRunMUSCLE；⚠️ 引擎硬编码 muscle3 -in/-out 语法在 v5 系统崩 → Python
  直调自动适配；依赖系统 muscle）'
capabilities:
- alignment
capabilities_ontology:
- alignment
- sequence
- sequence.alignment
dependencies:
- muscle
dependency_manifest:
- name: muscle
  type: binary
  required: true
  platforms:
  - linux
  - macos
relations:
  accepts:
  - FASTA
  produces:
  - ALIGNMENT
  next_step:
  - trimal
  - iqtree
inputs:
- name: fasta
  role: file
  format: fasta
  required: true
  note: ''
outputs:
- aln
