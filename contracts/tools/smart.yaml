name: smart
kind: manual
mode: manual
class: ''
xmx: 2g
runner: plot
group: seq
help: 'smart: smart <in.fa> <out.txt>   # SMART 域注释（插件 P00060 CLI 化；⚠️ 联网 POST EMBL
  ismart.embl.de，约 10-60s，输出域位置+类型）'
capabilities:
- domain
- annotation
capabilities_ontology:
- annotation
- domain
- genomics
- genomics.annotation
- sequence
- sequence.domain
relations:
  accepts:
  - FASTA
  - GFF3
  produces:
  - SEQUENCE_ARTIFACT
  related_to:
  - muscle
  - trimal
inputs:
- name: seq
  role: file
  format: fasta
  required: true
  note: ''
outputs:
- tsv
