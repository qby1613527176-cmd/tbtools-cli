name: docs
on:
  push:
    branches: [main]
    paths: ['docs/**', 'mkdocs.yml', 'README.md', 'tbtools_cli/command_metadata.json']
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - name: Regenerate command list (metadata 单一源)
        run: |
          pip install -q .
          python3 scripts/gen_metadata.py --render
          # 同步 docs/commands.md(mkdocs 页面)——防 metadata 更新后快照漂移
          python3 - <<'PYEOF'
gen = open('docs/_generated/commands.md', encoding='utf-8').read()
open('docs/commands.md', 'w', encoding='utf-8').write('# 命令清单(自动生成)

> 由 `scripts/gen_metadata.py --render` 生成,构建时刷新,勿手改。

' + gen)
PYEOF
      - name: Build docs
        run: |
          pip install -q mkdocs mkdocs-material
          cp README.md docs/readme-src.md 2>/dev/null || true
          mkdocs build
      - uses: actions/upload-pages-artifact@v3
        with: {path: site}
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: github-pages
    steps:
      - uses: actions/deploy-pages@v4
