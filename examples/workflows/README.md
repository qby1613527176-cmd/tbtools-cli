# Workflow Gallery

可执行工作流示例(`tbtools workflow run <dir>/workflow.yaml`):

| 目录 | 管线 | 步骤 |
|---|---|---|
| `deg-pipeline/` | DEG 可视化 | volcano → dehist |
| `synteny/` | 共线性 | dualsyn → msy |
| `gxf-ops/` | GXF 拆分 | gxfSplit |

更多工作流定义见 `ai/workflows.json`(4 个可规划流程)。
