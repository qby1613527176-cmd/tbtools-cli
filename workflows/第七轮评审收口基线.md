# 第七份评审(#52)收口基线 2026-09-24

## 已落地(P0/P1 全 18 项)
- P0-1 CI YAML 结构(test/test-windows 重组)
- P0-2 版本一致性门禁(TestVersionConsistency)
- P0-8 跨平台文件锁(fcntl/msvcrt/降级)
- P0-9 大文件 reflink 快照(CoW)
- P0-10 cleanup 只删 t0 后新文件(目录清单判定)
- P1-3 依赖单一源(STRUCT 派生)
- P1-13 错误码分层(TB1xx/2xx/3xx)
- P1-14 JSON Protocol envelope
- P1-17 dry-run 资源预估(memory/deps/network)
- P1-19/22 agent.policy + MCP 默认关 reflection
- P1-20 RPC crash-loop breaker
- 文档数字自动化(README_EN/CONTRIBUTING)

## 此前已落地(评审引用时已存在)
- CommandSpec 模型接管 metadata(P0-3 实质完成;残留:模型本身从 reg/tools/manual 构建, ENGINE_REGISTRY 降为实现层——协议产物全部经模型,符合"唯一投影源")
- InputSpec columns/ParamSpec/任意 artifact output/MCP dict+list 结构化
- security execution policy/RPC 静态发现/版本单一源/数字机器化

## 路线图(2026-09-24 更新: 首批已全部落地 ✅)
- ~~ADR-0007 Artifact/Workflow 一等公民~~ ✅(artifact.py/inspect 命令;workflow.py validate/plan/graph/run+$step.output 链)
- ~~MCP embedded 模式~~ ✅(TBTOOLS_MCP_EMBEDDED=1, 0.03s vs 0.1s)
- ~~core.py 继续拆~~ ✅(512→309 行; runtime/{i18n,env,validation} 独立)
- ~~能力 ontology 分层~~ ✅(ONTOLOGY 40+ 标签, capabilities_ontology, search 父类命中)
- 剩余: 自动修复(FailureSpec repair)/Workflow Gallery 扩充/输出语义验证/workflow resume(v2)

## 冻结纪律(评审 §50)
不再:加命令堆数量/加 alias/新 CLI 入口/第二份 metadata/第二套 Agent API
以后指标:294 Tool Contracts / Agent success rate,不再是"多少命令"
